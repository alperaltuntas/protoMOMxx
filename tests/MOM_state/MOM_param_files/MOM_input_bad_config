! Parameter file for the state initialization unit tests: an
! unimplemented thickness configuration.
THICKNESS_CONFIG = "benchmark"

! The state initialization now takes the Grid and the VerticalGrid rather than
! a struct of scalars copied out of them, so both have to be constructible here.
NK = 2
MAXIMUM_DEPTH = 2000.0
ANGSTROM = 1.0E-10
COORD_CONFIG = "gprime"
GFS = 0.98
GINT = 0.0098
